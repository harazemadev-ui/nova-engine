class ModuleContext:

    def __init__(
        self,
        message,
        userUID,
        chatUID,
        memories
    ):
        self.message = message
        self.userUID = userUID
        self.chatUID = chatUID
        self.memories = memories
